# Phase 0 — scaffold runs on these alone
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# Phase 2 — detection
numpy>=1.26
scipy>=1.13
opencv-python-headless>=4.10
rasterio>=1.3
shapely>=2.0
Pillow>=10.0

# temp-frontend upload endpoint (multipart form parsing)
python-multipart>=0.0.20

# Phase 3 — drift
xarray>=2024.6
netCDF4>=1.7

# Phase 4 — attribution
pandas>=2.2
pyarrow>=17.0

# Phase 6 — report
reportlab>=4.2

# Vessel attribution — optional Isolation Forest secondary anomaly detector
# (app/attribution/filters.py). Rule-based behaviour analysis is the primary,
# always-on path and needs none of this; only used when a caller explicitly
# passes use_isolation_forest=True.
scikit-learn>=1.5

# Phase 7 — optional U-Net detector (CPU inference; skip if you never trained one).
# torch/torchvision are pinned to a matched pair (letting them resolve
# independently can pull mismatched builds -- "torchvision::nms does not
# exist" is the symptom). Install from the CPU wheel index to avoid pulling
# CUDA packages the backend doesn't need:
#   uv pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
torchvision==0.21.0
segmentation-models-pytorch>=0.5

# Dev
pytest>=8.3
httpx>=0.27
